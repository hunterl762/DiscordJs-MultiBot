const { ActionRowBuilder,MessageFlags,ModalBuilder,SlashCommandBuilder,TextInputBuilder,TextInputStyle }=require('discord.js');const {getFeature}=require('../../src/features/store');
module.exports={name:'apply',category:'Applications',data:new SlashCommandBuilder().setName('apply').setDescription('Open a server application form.').addStringOption(o=>o.setName('type').setDescription('Application type').setMaxLength(40)),guildOnly:true,async executeSlash(i){const f=await getFeature(i.guildId,'applications');if(!f?.enabled)return i.reply({content:'Applications are disabled.',flags:MessageFlags.Ephemeral});const questions=String(f.config.questions||'Why do you want to apply?').split('|').map(x=>x.trim()).filter(Boolean).slice(0,5);const modal=new ModalBuilder().setCustomId(`application:submit:${encodeURIComponent(i.options.getString('type')||'General')}`).setTitle('Server Application');questions.forEach((q,n)=>modal.addComponents(new ActionRowBuilder().addComponents(new TextInputBuilder().setCustomId(`q${n}`).setLabel(q.slice(0,45)).setStyle(TextInputStyle.Paragraph).setRequired(true).setMaxLength(1000))));return i.showModal(modal);},async executePrefix(m){return m.reply('Use /apply to open the application form.');}};
