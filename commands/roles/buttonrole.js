const { ActionRowBuilder,ButtonBuilder,ButtonStyle,MessageFlags,PermissionFlagsBits,SlashCommandBuilder }=require('discord.js');const {getFeature}=require('../../src/features/store');
module.exports={name:'buttonrole',category:'Roles',data:new SlashCommandBuilder().setName('buttonrole').setDescription('Create a self-role button.').setDefaultMemberPermissions(PermissionFlagsBits.ManageRoles).addRoleOption(o=>o.setName('role').setDescription('Role to toggle').setRequired(true)).addStringOption(o=>o.setName('label').setDescription('Button label').setRequired(true).setMaxLength(80)).addChannelOption(o=>o.setName('channel').setDescription('Panel channel')).addStringOption(o=>o.setName('emoji').setDescription('Optional emoji')),guildOnly:true,async executeSlash(i){const f=await getFeature(i.guildId,'button_roles');if(!f?.enabled)return i.reply({content:'Button roles are disabled in the Feature Center.',flags:MessageFlags.Ephemeral});const role=i.options.getRole('role',true);if(!role.editable)return i.reply({content:'My bot role must be above that role.',flags:MessageFlags.Ephemeral});const ch=i.options.getChannel('channel')||i.guild.channels.cache.get(f.config.panelChannelId)||i.channel;if(!ch?.isTextBased())return i.reply({content:'Choose a text channel.',flags:MessageFlags.Ephemeral});const b=new ButtonBuilder().setCustomId(`selfrole:${role.id}`).setLabel(i.options.getString('label',true)).setStyle(ButtonStyle.Secondary);const emoji=i.options.getString('emoji');if(emoji)try{b.setEmoji(emoji);}catch{}await ch.send({content:'Choose your roles:',components:[new ActionRowBuilder().addComponents(b)]});return i.reply({content:`Role button posted in ${ch}.`,flags:MessageFlags.Ephemeral});},async executePrefix(m){return m.reply('Use /buttonrole to create role buttons.');}};
