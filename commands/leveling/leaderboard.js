const { EmbedBuilder, SlashCommandBuilder }=require('discord.js');const {leaderboard}=require('../../src/features/dataStore');const {getFeature}=require('../../src/features/store');
module.exports={name:'leaderboard',aliases:['lb'],category:'Leveling',data:new SlashCommandBuilder().setName('leaderboard').setDescription('Show the XP leaderboard.'),guildOnly:true,async executeSlash(i){const f=await getFeature(i.guildId,'leveling');if(!f?.enabled)return i.reply({content:'Leveling is disabled.',ephemeral:true});const rows=await leaderboard(i.guildId,10);return i.reply({embeds:[new EmbedBuilder().setColor(0x5865f2).setTitle('XP Leaderboard').setDescription(rows.length?rows.map((r,n)=>`**${n+1}.** <@${r.user_id}> — Level **${r.level}** • ${r.xp} XP`).join('\n'):'No XP data yet.')]});},async executePrefix(m){const rows=await leaderboard(m.guildId,10);return m.reply(rows.map((r,n)=>`${n+1}. <@${r.user_id}> — ${r.xp} XP`).join('\n')||'No XP data yet.');}};
