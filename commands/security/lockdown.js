const { ChannelType, MessageFlags, PermissionFlagsBits, SlashCommandBuilder }=require('discord.js');
module.exports={name:'lockdown',category:'Security',data:new SlashCommandBuilder().setName('lockdown').setDescription('Lock or unlock server text channels.').setDefaultMemberPermissions(PermissionFlagsBits.ManageGuild).addStringOption(o=>o.setName('action').setDescription('Action').setRequired(true).addChoices({name:'Lock',value:'lock'},{name:'Unlock',value:'unlock'})),guildOnly:true,async executeSlash(i){await i.deferReply({flags:MessageFlags.Ephemeral});const lock=i.options.getString('action',true)==='lock';let changed=0,failed=0;for(const ch of i.guild.channels.cache.values()){if(ch.type!==ChannelType.GuildText)continue;try{await ch.permissionOverwrites.edit(i.guild.roles.everyone,{SendMessages:lock?false:null},{reason:`Server ${lock?'lockdown':'unlock'} by ${i.user.tag}`});changed++;}catch{failed++;}}return i.editReply(`${lock?'Locked':'Unlocked'} ${changed} text channel(s). ${failed} failed.`);},async executePrefix(m){return m.reply('Use /lockdown for server lockdown controls.');}};
